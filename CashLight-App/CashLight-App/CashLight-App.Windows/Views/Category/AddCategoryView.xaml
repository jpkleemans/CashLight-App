<Page
    x:Class="CashLight_App.Views.Category.AddCategoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CashLight_App.Views.Category"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"    
    DataContext="{Binding Category, Source={StaticResource Locator}}"
    x:Name="_this">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox HorizontalAlignment="Left" Margin="147,270,0,0" TextWrapping="Wrap" Text="{Binding Text}" VerticalAlignment="Top" Width="385"/>
        <ComboBox HorizontalAlignment="Left" SelectedItem="{Binding CurrentType, Mode=TwoWay}" ItemsSource="{Binding TypeList}" Margin="147,345,0,0" VerticalAlignment="Top" Width="385" RenderTransformOrigin="0.25,0.506"/>
        <TextBox HorizontalAlignment="Left" Margin="147,419,0,0" TextWrapping="Wrap" Visibility="{Binding AmountEnabled}" Text="{Binding AmountEnabled}" VerticalAlignment="Top" RenderTransformOrigin="2.936,-0.081" Width="385"/>
    </Grid>
</Page>
