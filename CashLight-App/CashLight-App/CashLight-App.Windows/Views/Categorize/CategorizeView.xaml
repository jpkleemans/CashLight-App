<Page
    x:Name="pageRoot"
    x:Class="CashLight_App.Views.Categorize.CategorizeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CashLight_App.Views.Categorize"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Categorize, Source={StaticResource Locator}}">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Button Content="Vast" HorizontalAlignment="Center" Margin="0" Grid.Row="1" VerticalAlignment="Center" Background="#FF48D86D" Width="88" Command="{Binding ButtonCommand}" CommandParameter="1"/>
        <Button Content="Variabel" HorizontalAlignment="Center" Margin="0,100,0,0" Grid.Row="1" VerticalAlignment="Center" Background="OrangeRed" Command="{Binding ButtonCommand}" CommandParameter="2"/>
        <Button Content="Overig" HorizontalAlignment="Center" Margin="0,200,0,0" Grid.Row="1" VerticalAlignment="Center" Background="DarkBlue" Width="88" Command="{Binding ButtonCommand}" CommandParameter="3"/>

        <TextBlock HorizontalAlignment="Center" Margin="-100,60,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Naam: " VerticalAlignment="Top" FontSize="18"/>
        <TextBlock HorizontalAlignment="Center" Margin="100,60,0,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Name}" VerticalAlignment="Top" FontSize="18" Foreground="White"/>

        <TextBlock HorizontalAlignment="Center" Margin="-100,100,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Bedrag: " VerticalAlignment="Top" FontSize="18"/>
        <TextBlock HorizontalAlignment="Center" Margin="100,100,0,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Amount}" VerticalAlignment="Top" FontSize="18" Foreground="White"/>

        <TextBlock HorizontalAlignment="Center" Margin="-100,140,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Af/Bij: " VerticalAlignment="Top" FontSize="18"/>
        <TextBlock HorizontalAlignment="Center" Margin="100,140,0,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding AfBij}" VerticalAlignment="Top" FontSize="18" Foreground="White"/>
    </Grid>
</Page>